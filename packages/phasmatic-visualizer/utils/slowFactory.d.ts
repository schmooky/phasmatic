import { FlowData, FlowConfig } from '../types';
/**
 * Generates a flow visualization from a Phasmatic state machine class
 */
export declare function generateFlowFromStateMachine(stateMachineClass: any, config?: FlowConfig): FlowData;
